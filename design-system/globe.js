/* index.html에서 추출한 공용 조각 — 페이지에 <canvas id="globe">가 있으면 그린다.
   크기·위치·투명도는 각 페이지의 CSS가 정한다. */

/* ============================================================================
   자전하는 지구 — 구면 픽셀 렌더링
   ① 해안선(Natural Earth 110m 간략화)으로 등장방형 지구 텍스처를 한 번 만들고
   ② 원반의 각 픽셀에 대해 위경도와 음영을 미리 계산해 두고
   ③ 매 프레임에는 경도만 밀어서 텍스처를 샘플링한다 — 그래서 빠르다.
   지구는 서→동으로 자전하므로 지형이 화면 왼쪽에서 오른쪽으로 흐른다.
   ========================================================================== */
const LAND=[
"-180,69 -174.3,66.3 -169.9,66 -173,64.3 -179.9,65.9 -180,65 180,65 177.4,64.6 179.2,62.3 170.3,59.9 163.5,59.9 162,58.2 163.2,57.6 162.1,54.9 156.8,51 155.9,56.8 164.5,62.6 160.1,60.5 159.3,61.8 156.7,61.4 154.2,59.8 155,59.1 142.2,59 135.1,54.7 139.9,54.2 141.4,52.2 138.2,46.3 127.5,39.8 129.1,35.1 126.5,34.4 126.9,36.9 124.7,38.1 125.3,39.6 121.1,38.9 121.6,40.9 118,39.2 118.9,37.4 122.4,37.5 119.2,34.9 121.9,31.7 121.7,28.2 115.9,22.8 110.4,20.3 108.5,21.7 105.9,19.8 109.3,13.4 109.2,11.7 105.2,8.6 100.1,13.4 99.2,9.2 103,5.5 104.2,1.3 101.4,2.8 98.3,7.8 97.2,16.9 94.2,16 94.3,18.2 91.4,22.8 87,21.5 80.3,15.9 79.9,10.4 77.5,8 73.5,16 72.6,21.4 70.5,20.9 66.4,25.4 57.4,25.7 56.5,27.1 54.7,26.5 51.5,27.9 50.1,30.1 48,30 51.8,24 54,24.1 56.4,26.4 56.8,24.2 59.8,22.3 55.3,17.2 43.5,12.6 42.7,16.8 34.6,28.1 34.9,29.5 33.9,27.6 32.4,29.9 37.5,18.6 42.7,11.7 44.6,10.4 51.1,12 51,10.6 47.7,4.2 39.2,-4.7 40.8,-14.7 34.8,-19.8 35.5,-24.1 32.6,-25.7 32.2,-28.8 28.2,-32.8 19.6,-34.8 11.8,-18.1 13.7,-10.7 11.9,-5 8.8,-1.1 9.4,3.7 5.9,4.3 4.3,6.3 -9,4.8 -16.6,12.2 -17.6,14.7 -16.1,18.1 -17,21.9 -5.9,35.8 -2.2,35.2 9.5,37.4 11.1,36.9 10.3,33.8 19.1,30.3 21.5,32.8 33.8,31 36.2,36.7 27.6,36.7 26.2,39.5 33.5,42 38.3,40.9 41.7,42 36.7,45.2 39.1,47.3 35,46.3 36.3,45.1 33.9,44.4 32.5,45.3 33.3,46.1 30.7,46.6 27.7,42.6 28.8,41.1 22.6,40.3 24,37.7 22.5,36.4 19.5,41.7 13.1,45.7 12.6,44.1 18.5,40.2 16.9,40.4 16.1,38 15.4,40 8.9,44.4 3.1,43.1 -2.1,36.7 -8.9,36.9 -9.4,43 -1.4,44 -1.2,46 -4.6,48
"-87,79.7 -85.8,79.3 -90.8,78.2 -96.7,80.2 -92.4,81.3 -87,79.7",
"-45.2,-78 -43.3,-80 -54.2,-80.6 -48.7,-78 -45.2,-78",
"173,-40.9 174.2,-41.3 173.1,-43.9 169.3,-46.6 166.7,-46.2 173,-40.9",
"-108.2,76.2 -105.7,75.5 -112.2,74.4 -117.7,75.2 -115.4,76.5 -108.2,76.2",
"174.6,-36.2 178.5,-37.7 175.2,-41.7 173.8,-39.5 174.7,-37.4 172.6,-34.5 174.6,-36.2"
];

(function globe(){
  const cv = document.getElementById('globe');
  if (!cv) return;
  const ctx = cv.getContext('2d', { alpha: true });
  const RAD = Math.PI / 180;

  const rings = LAND.map(s => s.split(' ').map(p => { const c = p.split(','); return [+c[0], +c[1]]; }));

  /* ── ① 등장방형 텍스처 ─────────────────────────────────────────────── */
  const TW = 1024, TH = 512;
  const tex = (function buildTexture(){
    const c = document.createElement('canvas'); c.width = TW; c.height = TH;
    const g = c.getContext('2d');
    const X = lon => (lon + 180) / 360 * TW;
    const Y = lat => (90 - lat) / 180 * TH;

    // 바다 — 위도에 따라 색이 달라진다(적도가 밝고 극지가 어둡다)
    const og = g.createLinearGradient(0, 0, 0, TH);
    og.addColorStop(0.00, '#0c2440'); og.addColorStop(0.18, '#0d3358');
    og.addColorStop(0.50, '#10497b'); og.addColorStop(0.82, '#0d3358');
    og.addColorStop(1.00, '#0c2440');
    g.fillStyle = og; g.fillRect(0, 0, TW, TH);

    // 육지는 별도 캔버스에 그려 두고, 위도별 색을 source-atop으로 입힌다
    const lc = document.createElement('canvas'); lc.width = TW; lc.height = TH;
    const lg = lc.getContext('2d');

    // 날짜변경선을 넘는 폴리곤이 가로줄로 번지지 않도록 경도를 연속으로 펴 준다
    const unwrap = (ring) => {
      const out = [ring[0].slice()];
      for (let i = 1; i < ring.length; i++){
        let lon = ring[i][0]; const prev = out[i - 1][0];
        while (lon - prev >  180) lon -= 360;
        while (prev - lon >  180) lon += 360;
        out.push([lon, ring[i][1]]);
      }
      return out;
    };
    const unwrapped = rings.map(unwrap);

    lg.fillStyle = '#4a7a45';
    for (const shift of [-360, 0, 360]){
      for (const r of unwrapped){
        lg.beginPath();
        for (let i = 0; i < r.length; i++){
          const x = X(r[i][0] + shift), y = Y(r[i][1]);
          i ? lg.lineTo(x, y) : lg.moveTo(x, y);
        }
        lg.closePath(); lg.fill();
      }
    }

    // 위도별 식생대 — 사막(20~32°)·침엽수림·빙설
    lg.globalCompositeOperation = 'source-atop';
    const bg = lg.createLinearGradient(0, 0, 0, TH);
    const band = [[ 90,'#f2f6f8'],[ 76,'#e7eef2'],[ 66,'#6d7f63'],[ 52,'#4f7a49'],
                  [ 40,'#5e8449'],[ 31,'#a6935f'],[ 22,'#c0aa6d'],[ 12,'#5a8a44'],
                  [  0,'#3f7f3f'],[-12,'#4d8544'],[-24,'#b09a63'],[-36,'#6b8a4c'],
                  [-52,'#4f7a49'],[-66,'#8fa08c'],[-78,'#e7eef2'],[-90,'#f2f6f8']];
    for (const [lat, col] of band) bg.addColorStop(Math.min(1, Math.max(0, Y(lat) / TH)), col);
    lg.fillStyle = bg; lg.fillRect(0, 0, TW, TH);

    // 얼룩 — 완전히 균일한 색은 인쇄물처럼 보인다
    for (let i = 0; i < 2600; i++){
      const x = Math.random() * TW, y = Math.random() * TH, r = 3 + Math.random() * 26;
      lg.fillStyle = `rgba(${Math.random() < 0.5 ? '0,0,0' : '255,255,255'},${0.015 + Math.random() * 0.05})`;
      lg.beginPath(); lg.arc(x, y, r, 0, Math.PI * 2); lg.fill();
    }
    lg.globalCompositeOperation = 'source-over';

    // 대륙붕 — 해안선 바깥에 옅은 물색을 깔아 경계를 부드럽게
    g.save();
    g.strokeStyle = 'rgba(90,170,205,0.55)'; g.lineWidth = 5; g.lineJoin = 'round';
    for (const shift of [-360, 0, 360]){
      for (const r of unwrapped){
        g.beginPath();
        for (let i = 0; i < r.length; i++){
          const x = X(r[i][0] + shift), y = Y(r[i][1]);
          i ? g.lineTo(x, y) : g.moveTo(x, y);
        }
        g.closePath(); g.stroke();
      }
    }
    g.restore();

    g.drawImage(lc, 0, 0);

    // 극관 — 데이터가 닿지 않는 위도까지 얼음으로 덮는다
    const cap = g.createLinearGradient(0, 0, 0, Y(72));
    cap.addColorStop(0, 'rgba(244,249,252,0.95)'); cap.addColorStop(1, 'rgba(244,249,252,0)');
    g.fillStyle = cap; g.fillRect(0, 0, TW, Y(72));
    const cap2 = g.createLinearGradient(0, TH, 0, Y(-68));
    cap2.addColorStop(0, 'rgba(244,249,252,0.98)'); cap2.addColorStop(1, 'rgba(244,249,252,0)');
    g.fillStyle = cap2; g.fillRect(0, Y(-68), TW, TH - Y(-68));

    return g.getImageData(0, 0, TW, TH).data;
  })();

  /* ── ①-2 구름층 ────────────────────────────────────────────────────
     우주에서 찍은 지구가 '사진처럼' 보이는 결정적 차이는 구름이다.
     값 노이즈를 여러 겹 쌓되, 위도별 구름대를 곱한다 — 적도 수렴대와
     한대 전선에는 구름이 몰리고 아열대 고압대(사막 위도)는 비어 있다.
     실제 대기 대순환과 같은 배치라 수업에서 그대로 짚어줄 수 있다. */
  const CW = 512, CH = 256;
  const cloudTex = (function buildClouds(){
    const out = new Uint8Array(CW * CH);
    const mk = (n) => { const g = new Float32Array(n * n); for (let i = 0; i < g.length; i++) g[i] = Math.random(); return g; };
    // 경도 방향은 순환, 위도 방향은 고정 — 이음매가 생기지 않도록
    function noise(g, n, u, v){
      const x = u * n, y = v * n;
      const x0 = Math.floor(x), y0 = Math.min(Math.floor(y), n - 1);
      const fx = x - x0, fy = y - y0;
      const xa = ((x0 % n) + n) % n, xb = (xa + 1) % n;
      const ya = y0 * n, yb = Math.min(y0 + 1, n - 1) * n;
      const sx = fx * fx * (3 - 2 * fx), sy = fy * fy * (3 - 2 * fy);
      const top = g[ya + xa] + (g[ya + xb] - g[ya + xa]) * sx;
      const bot = g[yb + xa] + (g[yb + xb] - g[yb + xa]) * sx;
      return top + (bot - top) * sy;
    }
    const oct = [[6, 0.50], [12, 0.26], [24, 0.15], [48, 0.09]];
    const grids = oct.map(o => mk(o[0]));
    for (let y = 0; y < CH; y++){
      const lat = 90 - (y + 0.5) / CH * 180, al = Math.abs(lat);
      const band = 0.42
                 + 0.58 * Math.exp(-Math.pow(al / 14, 2))            // 적도 수렴대
                 + 0.52 * Math.exp(-Math.pow((al - 55) / 20, 2))     // 한대 전선
                 - 0.30 * Math.exp(-Math.pow((al - 27) / 12, 2));    // 아열대 고압대(사막 위도)
      const v = (y + 0.5) / CH;
      for (let x = 0; x < CW; x++){
        const u = (x + 0.5) / CW;
        let n = 0;
        for (let k = 0; k < oct.length; k++) n += noise(grids[k], oct[k][0], u, v) * oct[k][1];
        let a = n * band * 2.3 - 0.34;
        a = a <= 0 ? 0 : a >= 1 ? 1 : a * a * (3 - 2 * a);
        out[y * CW + x] = a * 255;
      }
    }
    return out;
  })();

  /* ── ② 픽셀별 위경도·음영 미리 계산 ─────────────────────────────────
     내부 렌더 해상도는 실제 표시 크기에 맞춰 정한다. 폰에서 255px로 보이는
     구를 560px로 그리면 픽셀 연산이 네 배가 되어 프레임을 떨어뜨린다. */
  const TILT = 20 * RAD;               // 살짝 위에서 내려다본 각도
  const sinT = Math.sin(TILT), cosT = Math.cos(TILT);

  // 빛은 왼쪽 위 앞에서
  const L = (function(){ const v = [-0.46, 0.40, 0.79]; const m = Math.hypot(...v); return v.map(k => k / m); })();

  let S, R, CX, CY, img, buf, idx, rowOff, colBase, shade, rim, cRow, cCol, N = 0;

  function setup(){
    const shown = cv.clientWidth || 400;
    S = Math.min(560, Math.max(320, Math.round(shown * 1.25)));
    cv.width = cv.height = S;
    R = S * 0.43; CX = S / 2; CY = S / 2;
    img = ctx.createImageData(S, S);
    buf = img.data;
    N = 0;
    const cap = S * S;
    const _idx = new Int32Array(cap), _row = new Int32Array(cap), _col = new Float32Array(cap);
    const _sh = new Float32Array(cap), _rm = new Float32Array(cap);
    const _cr = new Int32Array(cap), _cc = new Float32Array(cap);
    for (let py = 0; py < S; py++){
      for (let px = 0; px < S; px++){
        const x = (px + 0.5 - CX) / R, y = -(py + 0.5 - CY) / R;
        const rho2 = x * x + y * y;
        if (rho2 > 1) continue;
        const nz = Math.sqrt(1 - rho2);                 // 시선 방향 법선 성분
        const rho = Math.sqrt(rho2);
        const c = Math.asin(Math.min(1, rho));
        const sinc = Math.sin(c), cosc = Math.cos(c);
        const lat = rho < 1e-9 ? TILT / RAD
                  : Math.asin(cosc * sinT + (y * sinc * cosT) / rho) / RAD;
        const lonRel = rho < 1e-9 ? 0
                  : Math.atan2(x * sinc, rho * cosc * cosT - y * sinc * sinT) / RAD;

        // 음영 — 램버트 + 환경광, 가장자리는 프레넬로 대기가 밝아진다
        const diff = Math.max(0, x * L[0] + y * L[1] + nz * L[2]);
        const fres = Math.pow(1 - nz, 3);
        _idx[N] = (py * S + px) * 4;
        _row[N] = Math.min(TH - 1, Math.max(0, Math.round((90 - lat) / 180 * TH))) * TW;
        _col[N] = (lonRel + 180) / 360 * TW;
        _cr[N]  = Math.min(CH - 1, Math.max(0, Math.round((90 - lat) / 180 * CH))) * CW;
        _cc[N]  = (lonRel + 180) / 360 * CW;
        _sh[N]  = 0.20 + 0.95 * Math.pow(diff, 0.85);   // 환경광 0.20
        _rm[N]  = fres * 1.00 + Math.pow(diff, 6) * 0.25;
        N++;
      }
    }
    idx = _idx.subarray(0, N); rowOff = _row.subarray(0, N);
    colBase = _col.subarray(0, N); shade = _sh.subarray(0, N); rim = _rm.subarray(0, N);
    cRow = _cr.subarray(0, N); cCol = _cc.subarray(0, N);
  }
  setup();

  /* ── ③ 매 프레임: 경도만 밀어서 샘플링 ───────────────────────────── */
  const RIM = [130, 185, 245];         // 대기 산란 색
  let lon0 = 128;                      // 처음엔 한반도가 정면에
  let cloudLon = 128;                  // 구름은 지표보다 아주 조금 빠르게 흐른다

  function draw(){
    const shift = lon0 / 360 * TW;
    const cShift = cloudLon / 360 * CW;
    for (let i = 0; i < N; i++){
      let col = colBase[i] + shift;
      col %= TW; if (col < 0) col += TW;
      const s = (rowOff[i] + (col | 0)) * 4;

      let cc = cCol[i] + cShift;
      cc %= CW; if (cc < 0) cc += CW;
      const a = cloudTex[cRow[i] + (cc | 0)] * 0.00392;   // 0~1

      const sh = shade[i], rm = rim[i], o = idx[i];
      // 지표색 위에 구름을 얹고, 그 결과에 음영과 대기광을 입힌다
      buf[o]     = Math.min(255, (tex[s]     + (252 - tex[s])     * a) * sh + RIM[0] * rm);
      buf[o + 1] = Math.min(255, (tex[s + 1] + (253 - tex[s + 1]) * a) * sh + RIM[1] * rm);
      buf[o + 2] = Math.min(255, (tex[s + 2] + (255 - tex[s + 2]) * a) * sh + RIM[2] * rm);
      buf[o + 3] = 255;
    }
    ctx.putImageData(img, 0, 0);

    // 바깥 대기 헤일로 — 구가 상단바에 닿지 않으므로, 유리가 굴절할 대상은
    // 이 헤일로다. 그래서 조금 진하게 잡는다.
    const halo = ctx.createRadialGradient(CX, CY, R, CX, CY, R * 1.15);
    halo.addColorStop(0, 'rgba(122,176,235,0.46)');
    halo.addColorStop(1, 'rgba(122,176,235,0)');
    ctx.beginPath(); ctx.arc(CX, CY, R * 1.15, 0, Math.PI * 2);
    ctx.fillStyle = halo; ctx.fill();
  }

  const PERIOD = 80;                   // 1회전 80초
  let last = 0;
  function frame(now){
    if (!last) last = now;
    const dt = Math.min((now - last) / 1000, 0.05);
    last = now;
    lon0 -= (360 / PERIOD) * dt;       // 서→동 자전
    cloudLon -= (360 / PERIOD) * 1.09 * dt;
    if (cloudLon < -180) cloudLon += 360;
    if (lon0 < -180) lon0 += 360;
    draw();
    requestAnimationFrame(frame);
  }

  draw();
  if (!matchMedia('(prefers-reduced-motion: reduce)').matches) requestAnimationFrame(frame);

  // 화면 회전·창 크기 변경으로 표시 크기가 크게 달라지면 해상도를 다시 잡는다
  let rt;
  addEventListener('resize', () => {
    clearTimeout(rt);
    rt = setTimeout(() => {
      const want = Math.min(560, Math.max(320, Math.round((cv.clientWidth || 400) * 1.25)));
      if (Math.abs(want - S) > 40) { setup(); draw(); }
    }, 250);
  });
})();
